import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
    container:{
        padding: 8,
        borderRadius: 16,
        elevation: 10,
        zIndex: 10
    },
    text:{
        fontWeight: 'normal',
        fontSize: 16
    }
});

export default styles;
