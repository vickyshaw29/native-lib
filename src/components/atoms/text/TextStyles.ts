import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  textContainer: {
    margin: 10,
  },
  text: {
    fontSize: 20,
    fontWeight: '500',
  },
});

export default styles;
