import {StyleSheet} from 'react-native'
const styles = StyleSheet.create({
    mainContainer:{
        // marginTop: 16,
        // marginBottom: 16,
        // paddingVertical: 16
    }
});

export default styles